[Unit]
# The graphical greeter must never pre-empt tty1 while appliance ownership is
# still being established. The condition is evaluated by the system manager.
ConditionPathExists=/var/lib/beam-appliance/firstboot-complete
